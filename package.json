{
  "name": "wgo-app",
  "version": "1.0.0",
  "engines": {
    "node": "12.x"
  },
  "main": "index.js",
  "author": "Ola Podsiadlik <alecs@op.pl>",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.4",
    "yarn": "1.22.5"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "start": "npm-run-all -p -l start:*",
    "start:server": "cd server && server.js",
    "start:client": "cd client && npm start",
    "install:client": "cd client && npm install",
    "install:server": "cd server && npm install",
    "postinstall": "npm-run-all -p install:*"
  }
}
